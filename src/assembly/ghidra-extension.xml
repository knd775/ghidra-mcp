<?xml version="1.0" encoding="UTF-8" ?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 &#xA;        http://maven.apache.org/xsd/assembly-1.1.3.xsd">
    <!-- Just a name for reference -->
    <id>ghidra-extension</id>
    <!-- We want a .zip file -->
    <formats>
        <format>zip</format>
    </formats>
    <!-- Don't put everything in an extra top-level directory named after the assembly ID -->
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <!-- 1) Copy extension.properties from target/classes (filtered with version substitution) -->
        <fileSet>
            <directory>${project.build.outputDirectory}</directory>
            <includes>
                <include>extension.properties</include>
            </includes>
            <outputDirectory>GhidraMCP</outputDirectory>
        </fileSet>
        <!-- 2) Copy Module.manifest from source (not filtered) -->
        <fileSet>
            <directory>src/main/resources</directory>
            <includes>
                <include>Module.manifest</include>
            </includes>
            <outputDirectory>GhidraMCP</outputDirectory>
        </fileSet>
        <!-- 3) Copy your built plugin JAR into GhidraMCP/lib -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <includes>
                <!-- Use the finalized JAR name from the maven-jar-plugin -->
                <include>${project.build.finalName}.jar</include>
            </includes>
            <outputDirectory>GhidraMCP/lib</outputDirectory>
        </fileSet>
        <!-- PostgreSQL JDBC for the corroboration companion schema. -->
        <fileSet>
            <directory>${project.build.directory}/lib</directory>
            <includes>
                <include>postgresql-*.jar</include>
            </includes>
            <outputDirectory>GhidraMCP/lib</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>